---
title: "Analiza trendów zakupowych"
author: "Anhelina Beliak, Dominika Harasimiuk, Zuzanna Kopeć"
date: "`r Sys.Date()`"
output:
  rmdformats::robobook:
    highlight: kate
    toc_depth: 3
    self_contained: true
    number_section: true
    code_folding: show
---

```{r setup, include=FALSE, error=FALSE}
## Global options
knitr::opts_chunk$set(cache = TRUE)
dane <- read.csv("trendy_zakupowe_new.csv")
library(tidyverse)
library(naniar)
library(mice)
library(validate)
library(validatetools)
library(dcmodify)
library(errorlocate)
library(dplyr)
library(ggplot2)
library(psych)
library(ggstatsplot)
library(broom)
library(kableExtra)
library(ggplot2)
library(mice)
library(knitr)
library(kableExtra)
library(misty)
library(rstatix)
library(usmap)
library(plotly)
library(psych)
library(qwraps2)
library(EnvStats)
library(data.table) # data mgmt
library(gtools) # combination
library(ggplot2) # graphics
library(plotly) # interactive graphics
library(corrplot)
library(VIM)
library(patchwork)
library(gridExtra)
library(lattice)
```

```{r, include=FALSE}
dane <- rename(dane, 
    ID = Customer.ID,
    Item = Item.Purchased,
    Amount = Purchase.Amount..USD.,
    Rating = Review.Rating,
    Subscription = Subscription.Status,
    Shipping = Shipping.Type,
    Discount = Discount.Applied,
    Code = Promo.Code.Used,
    Payment = Payment.Method,
    Frequency = Frequency.of.Purchases
)
```

# Wprowadzenie

::: {style="text-align: justify;"}
Celem projektu jest analiza danych sprzedażowych, której zadaniem jest identyfikacja oraz zrozumienie wzorców zakupowych i preferencji klientów. Analiza ta ma na celu wyszczególnienie zależności występujących w danych, określenie charakterystyki zachowań konsumenckich oraz wskazanie czynników wpływających na decyzje zakupowe.

Wykorzystane zostały metody wstępnego przetwarzania danych, w szczególności techniki czyszczenia danych obejmujące identyfikację i eliminację braków danych.Zastosowane zostały narzędzia analizy danych, w tym techniki wizualizacji, umożliwiające przedstawienie rozkładów zmiennych, relacji pomiędzy nimi oraz identyfikację istotnych trendów.

Kluczowym elementem badania będzie również zastosowanie metod wnioskowania statystycznego, pozwalających na formułowanie i weryfikację hipotez badawczych oraz ocenę istotności zaobserwowanych zależności.

Przeprowadzona analiza ma na celu udzielenie odpowiedzi na sformułowane pytania badawcze, a także dostarczenie wniosków mogących stanowić podstawę do podejmowania decyzji biznesowych oraz dalszych badań analitycznych.
:::

## Opis danych

::: {style="text-align: justify;"}
Analizowany zbiór danych zawiera 3900 obserwacji i 18 zmiennych. W tabeli poniżej zostały przedstawione wszystkie zmienne wraz z informacjami na temat typu zmiennej, opisem i wartościami jakie przyjmuje.
:::

```{r}
data_dict <- data.frame(
  Zmienna = c("Customer ID", "Age", "Gender", "Item", "Category", 
              "Amount", "Location", "Size", "Color", 
              "Season", "Rating", "Subscription", "Shipping", "Discount", "Code", "Previous.Purchases", "Payment", "Frequency"),
  Typ = c("Jakościowa (nominalna)", "Ilościowa (dyskretna)", "Jakościowa (binarna)", "Jakościowa (Nominalna)",
          "Jakościowa (nominalna)", "Ilościowa (ciągła)", "Jakościowa (nominalna)", 
          "Jakościowa (porządkowa)", "Jakościowa (nominalna)", "Jakościowa (nominalna)", "Ilościowa (ciągła)", "Jakościowa (binarna)", "Jakościowa (nominalna)", "Jakościowa (binarna)", "Jakościowa (binarna)", "Ilościowa (ciągła)", "Jakościowa (nominalna)", "Jakościowa (nominalna)" ),
  Opis = c("Unikalny identyfikator","Wiek klienta w latach", "Płeć klienta", "Przedmiot zakupiony przez klienta", "Grupa produktów", "Wartość transakcji (USD)","Lokalizacja, w której dokonano zakupu","Rozmiar produktu (S-XL)", "Kolor zakupionego przedmiotu", "Pora roku zakupu","Ocena produktu (1-5)","Wskazuje, czy klient posiada subskrypcję","Rodzaj wysyłki wybrany przez klienta" 
           ,"Wskazuje, czy do zakupu zastosowano rabat" ,"Wskazuje, czy do zakupu użyto kodu promocyjnego", "Łączna liczba transakcji dokonanych przez klienta w sklepie, z wyłączeniem bieżącej transakcji", "Najbardziej preferowana metoda płatności klienta", "Częstotliwość, z jaką klient dokonuje zakupów" ),
  Wartości = c("Od 1 do 3800", "W latach", "Male, Female", "Backpack, Belt, Blouse, Boots, Coat, Dress, Gloves, Handbag, Hat, Hoodie, Jacket, Jeans, Jewelry, Pants, Sandals, Scarf, Shirt, Shoes, Shorts, Skirt, Sneakers, Socks, Sunglasses, Sweater, T-shirt","Accessories, Clothing, Footwear, Outerwear.", "Wartość liczbowa",  "50 stanów", "L,M,S, XL", "Beige, Black, Blue, Brown, Charcoal, Cyan, Gold, Gray, Green, Indigo, Lavender, Magenta, Maroon, Olive, Orange, Peach, Pink, Purple, Red, Silver, Teal, Turquoise, Violet, White, Yellow.","Fall, Spring, Summer, Winter.", "Wartość od 1 do 5", "Yes, No", "2-Day Shipping, Express, Free Shipping, Next Day Air, Standard, Store Pickup.", "Yes, No","Yes, No", "Wartość liczbowa", "Bank Transfer, Cash, Credit Card, Debit Card, PayPal, Venmo.", "Annually, Bi-Weekly, Every 3 Months, Fortnightly, Monthly, Quarterly, Weekly."  )
)

kable(data_dict, 
      caption = "Tabela 1: Specyfikacja zmiennych w zbiorze danych",
      booktabs = TRUE, 
      align = "llll") %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed", "responsive"),
                full_width = F, 
                position = "left") %>%
  column_spec(1, bold = TRUE, border_right = TRUE) %>%
  column_spec(2, italic = TRUE)

```

# Data wrangling

## Braki danych

::: {style="text-align: justify;"}
Pierwszym etapem przeprowadzonej analizy danych było zweryfikowanie kompletności badanego zbioru danych, w szczególności identyfikacja występowania braków danych. W tym celu dokonano oceny liczby obserwacji niekompletnych oraz ich udziału procentowego w całym zbiorze.

Przeprowadzona analiza wykazała, że w badanym zbiorze danych występuje `r n_miss(dane)` obserwacji zawierających braki danych, co stanowi `r pct_miss(dane)`% wszystkich obserwacji, natomiast `r n_complete(dane)` obserwacji stanowią dane kompletne. W kolejnym kroku dokonano szczegółowej analizy braków danych wraz z badaniem struktury oraz wzorców występowania braków danych, w celu ustalenia, czy braki pojawiają się w sposób losowy, czy też wykazują określone zależności pomiędzy zmiennymi. Identyfikacja wzorców braków danych umożliwia podjęcie decyzji dotyczących dalszego postępowania z brakującymi obserwacjami, takich jak ich imputacja.
:::

### Wzorzec braków danych

::: {style="text-align: justify;"}
Pierwszym krokiem w analizie wzorców braków danych jest graficzne przedstawienie braków danych. Poniższy wykres wskazuje, że chociaż największa liczba obserwacji charakteryzuje się brakiem pojedynczej zmiennej, to występują również wyraźne przypadki jednoczesnych braków w więcej niż jednej zmiennej. Analiza mapy braków danych wskazuje na istnienie powtarzalnych wzorców współwystępowania braków w zmiennych: wiek (Age), wydatki (Amount oraz sezon (Season), co sugeruje, że braki danych nie są całkowicie losowe.
:::

```{r, warning=FALSE}
naniar::gg_miss_upset(dane) 

```

::: {style="text-align: justify;"}
W celu lepszego zobrazowania powiązań między brakami danych w trzech kluczowych zmiennych, zestawiono ze sobą odpowiednie wykresy. Analiza wizualna pozwala stwierdzić, że braki w informacjach dotyczących wieku oraz wydanej kwoty występują niemal wyłącznie w przypadku zakupów dokonywanych jesienią. Świadczy to o tym, że w tej kategorii braki nie mają charakteru losowego.

Z kolei analiza relacji pomiędzy samym wiekiem a wydaną kwotą pokazuje, że braki danych są rozproszone równomiernie: występują w całym zakresie kwot oraz u osób w każdym przedziale wiekowym. Oznacza to brak korelacji w występowaniu braków między tymi dwiema zmiennymi.
:::

```{r warning=FALSE}

p1 <- ggplot(dane, aes(x=Season, y=Age)) +
  geom_miss_point() +
  labs(title="Season i Age",
       x="Season",
       y="Age") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

p2 <- ggplot(dane, aes(x=Season, y=Amount)) +
  geom_miss_point() +
  labs(title="Season i Amount",
       x="Season",
       y="Amount") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

p3 <- ggplot(dane, aes(x=Age, y=Amount)) +
  geom_miss_point() +
  labs(title="Age i Amount",
       x="Age",
       y="Amount")
(p1 + p2) / p3

```

### Imputacje metodą hotdeck

::: {style="text-align: justify;"}
Imputacja metodą hotdeck polega na uzupełnianiu brakujących wartości poprzez przypisanie im rzeczywistych obserwacji pochodzących od statystycznie podobnych jednostek w tym samym zbiorze danych. Dobór tzw. dawcy odbywa się na podstawie zmiennych pomocniczych, które są kompletne i silnie powiązane z imputowaną cechą. W przeprowadzonej analizie wykorzystano imputację hotdeck w celu ograniczenia skali braków danych bez wprowadzania sztucznych wartości.
:::

```{r, include=FALSE}
dane_imp <- hotdeck(dane)
naniar::vis_miss(dane_imp)
#dane bez braków
dane_kompletne <- dane_imp[,1:18]

```

::: {style="text-align: justify;"}
Po przeprowadzonej imputacji metodą hotdeck, zbiór danych został uzupełniony o brakujące wartości, co pozwoliło na uzyskanie kompletnego zestawu danych do dalszych analiz. Jak można zauważyć na poniższym wykresie, po zastosowaniu tej metody, zbiór danych nie zawiera już braków danych, co umożliwia przeprowadzenie dalszych analiz bez konieczności uwzględniania problemu niekompletności danych.
:::

```{r}
#sprawdzenie czy dane_kompletne nie zawierają braków danych
vis_miss(dane_kompletne, cluster = TRUE) + 
theme(
    axis.text.x = element_text(angle = 45, hjust = 0, vjust = 0.5, size = 7),
    plot.margin = margin(t = 60, r = 10, b = 10, l = 10),
    plot.title = element_text(hjust = 0, face = "bold", size = 14),
  ) +
  labs(title = "Analiza braków danych")
```

::: {style="text-align: justify;"}
W celu sprawdzenie, czy zastosowana metoda imputacji hotdeck była skuteczna, przeprowadzono wizualną ocenę zgodności rozkładu zmiennych Age, Season i Amount przed i po imputacji. Poniższy wykres przedstawia porównanie rozkładów zmiennych w oryginalnym zbiorze danych z brakami oraz w zbiorze danych po imputacji. Analiza wizualna wskazuje, że rozkład zmiennych po imputacji jest zbliżony do rozkładu oryginalnego, co sugeruje, że metoda hotdeck skutecznie zachowała charakterystyki statystyczne tej zmiennej.
:::

```{r, include=FALSE}

p4 <- stripplot(dane_imp, Age ~ Age_imp, pch=20, cex=1.2, scales=list(y=list(at=seq(0, 70, by=10))))
p5 <- stripplot(dane_imp, Amount ~ Amount_imp, pch=20, cex=1.2,scales=list(y=list(at=seq(0, 100, by=10))))
p6 <- stripplot(dane_imp, Season ~ Season_imp, pch=20, cex=1.2)

grid.arrange(p4, p5, p6, ncol = 3)

```

## Walidacja danych

::: {style="text-align: justify;"}
Przeprowadzona została walidacja danych, której celem jest zapewnienie poprawności, spójności oraz wiarygodności zbioru danych wykorzystywanego w dalszych analizach. Przeprowadzono weryfikację danych pod kątem zgodności wartości zmiennych z przyjętymi założeniami merytorycznymi oraz logicznymi ograniczeniami wynikającymi z charakteru analizowanego zjawiska. Sprawdzono m.in. poprawność zakresów zmiennych liczbowych, a także zgodność zmiennych jakościowych z dopuszczalnymi kategoriami. Przeprowadzone działania walidacyjne pozwoliły na przygotowanie spójnego i rzetelnego zbioru danych do dalszych etapów analizy.
:::

W ramach walidacji danych przyjęto następujące kryteria poprawności zmiennych:

-   Wiek (age) – wartości mieszczące się w przedziale od 0 do 120 lat.
-   Płeć (gender) – zmienna jakościowa ograniczona do kategorii female oraz male.
-   Kwota zakupu (purchase amount) – wartości dodatnie, większe od zera.
-   Rozmiar (size) – dopuszczalne kategorie: XS, S, M, L, XL.
-   Ocena opinii (review rating) – wartości z przedziału od 0 do 5.
-   Liczba wcześniejszych zakupów (previous purchases) – wartości dodatnie, większe od zera.
-   Zastosowanie rabatu (discount applied) – zmienna binarna: tak lub nie.
-   Użycie kodu promocyjnego (promo code use) – zmienna binarna: tak lub nie.
-   Subskrypcja (subscription) – zmienna binarna: tak lub nie.

```{r, include=FALSE}
rules <- validator(
Discount %in% c("Yes","No"),
Code %in% c("Yes","No"),
Subscription %in% c("Yes","No"),
Size %in% c("XS","S","M","L","XL"),
Rating >= 0 & Rewiev.Rating <= 5,
Amount >= 0,
Gender %in% c("Female","Male"),
Age >= 0 & Age <= 120,
Previous.Purchases >=0
)
```

```{r warning=FALSE}
cf <- confront(dane_kompletne, rules, key="ID")

barplot(cf)

```

::: {style="text-align: justify;"}
Przeprowadzona walidacja danych wskazała, że wszystkie obserwacje w zbiorze danych spełniają przyjęte kryteria poprawności dla poszczególnych zmiennych. Oznacza to, że dane są spójne i zgodne z założeniami merytorycznymi, co pozwala na ich dalsze wykorzystanie w analizie bez konieczności wprowadzania dodatkowych korekt czy eliminacji obserwacji.
:::

# Analiza opisowa

## Statystyki opisowe zmiennych ilościowych

::: {style="text-align: justify;"}
Kolejnym etapem badania jest szczegółowa analiza statystyczna zmiennej ilościowej Amount, która reprezentuje kwoty wydawane przez klientów podczas pojedynczej transakcji. Celem poniższego zestawienia jest nie tylko wyznaczenie przeciętnej wartości koszyka zakupowego, ale również zbadanie struktury tych wydatków – ich zróżnicowania, asymetrii oraz koncentracji wokół średniej.
:::

```{r warning=FALSE}
raport <- list(
  "Wydatki (USD)" = list(
    "Min"                  = ~ min(Amount),
    "Max"                  = ~ max(Amount),
    "Kwartyl dolny"        = ~ quantile(Amount, 0.25),
    "Mediana"              = ~ round(median(Amount), 2),
    "Kwartyl górny"        = ~ quantile(Amount, 0.75),
    "Średnia"              = ~ round(mean(Amount), 2),
    "Odch. std."           = ~ round(sd(Amount), 2),
    "IQR"                  = ~ round(IQR(Amount), 2),
    "Odchylenie ćwiartkowe" = ~ round(IQR(Amount) / 2, 2),
    "Odch. std. w %"       = ~ round((sd(Amount) / mean(Amount)) * 100, 2),
    "Odch. ćwiartkowe w %" = ~ round((IQR(Amount) / median(Amount)) * 100, 2),
    "Skośność"             = ~ round(skewness(Amount), 2),
    "Kurtoza"              = ~ round(kurtosis(Amount), 2)
  )
)

tabela_wynikowa <- summary_table(dane_kompletne, summaries = raport, by = "Category")

kbl(tabela_wynikowa, 
    caption = "Szczegółowa analiza statystyczna wydatków",
    booktabs = TRUE, 
    align = "c") %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed"),
    full_width = FALSE,
    position = "center",
    font_size = 13
  ) %>%
  column_spec(1, bold = TRUE, border_right = TRUE) %>%

  add_header_above(c(" " = 1, "Kategorie produktów" = ncol(tabela_wynikowa) - 1))
```

::: {style="text-align: justify;"}
Z otrzymanych wyników można wywnioskować, że wydatki na różne kategorie produktów rozkładają się równomiernie, ponieważ Mediana i Średnia mają pododne wartości. Odchylenia standardowe w przypadku Akcesorii, Ubrań i Obuwia są bardzo zbliżone do siebie, co świadczy o tym, że klienci wydają podobne kwoty na te kategorie. Natomiast, widać trochę większe odchylenie w przypadku Odzieży wierzchniej, co świadczy o tym, że w przypadku tej kategorii klienci wydają trochę więcej. Skośność bliska zeru wskazuje na symetryczny rozkład wydatków klientów. Sugeruje to, że struktura asortymentowa sklepu jest zbalansowana, a klienci równie chętnie wybierają produkty z dolnej, jak i górnej półki cenowej.Wyniki kurtozy oznaczają, że wydatki nie są mocno skoncentrowane wokół jednej kwoty, ale są dość równomiernie "rozsiane".
:::

```{r warning=FALSE}
library(gtsummary)
library(reactable) 
summary_tbl <- dane_kompletne %>% 
  select(Age, Amount, Rating, Previous.Purchases) %>% 
  summarise( 
    Wiek = paste0(round(mean(Age), 2), " (", round(sd(Age), 2), ")"), 
    Kwota = paste0(round(mean(Amount), 2), " (", round(sd(Amount), 2), ")"), 
    Ocena = paste0(round(mean(Rating), 2), " (", round(sd(Rating), 2), ")"), 
    `Kwota wydana na wcześniejszych zakupach` = paste0(round(mean(Previous.Purchases), 2), " (", round(sd(Previous.Purchases), 2), ")") ) %>% 
  pivot_longer(cols = everything(), names_to = "Zmienna", values_to = "Średnia (SD)")  

reactable(summary_tbl, 
          bordered = TRUE, 
          highlight = TRUE, 
          striped = TRUE, 
          defaultColDef = colDef(align = "center") 
          )
```

::: {style="text-align: justify;"}
Na podstawie wyników można stwierdzić, że średni wiek badanych wynosi 44 lata, przy dużym zróżnicowaniu (SD = 15.22), co wskazuje na udział osób z różnych grup wiekowych. Średnia kwota zakupu to 59,61 USD, a wcześniejsze wydatki wynosiły przeciętnie 25,35 USD, co może sugerować wzrost zainteresowania ofertą lub skuteczność działań promocyjnych. Oceny produktów są raczej pozytywne (średnia 3,75 w skali 1–5), przy umiarkowanej zgodności opinii (SD = 0,72). Wszystkie zmienne wykazują znaczną zmienność, co świadczy o różnorodności zachowań konsumenckich w badanej grupie.
:::

# Wizualizacja danych

::: {style="text-align: justify;"}
Wizualizacja danych stanowi istotny element procesu analizy danych, umożliwiający przejrzyste i intuicyjne przedstawienie informacji zawartych w zbiorze danych.

W niniejszym rozdziale zaprezentowano wybrane metody wizualizacji danych wykorzystane w celu lepszego zrozumienia analizowanych zjawisk.
:::

W ramach przeprowadzonej analizy sformułowano następujące pytania badawcze:

1.  Czy wiek konsumentów wpływa na wybór kategorii zakupionych przedmiotów?

2.  Czy wiek konsumentów wpływa na wysokość kwoty zakupu?

3.  Jakie są najpopularniejsze kombinacje rozmiarów i kolorów w poszczególnych kategoriach?

4.  Jakie są preferencje klientów dotyczące metod płatności w zależności od grupy wiekowej?

5.  Czy klienci wydający więcej wybierają szybsze opcje wysyłki?

6.  Czy większa liczba klientów wpływa na wyższą sprzedaż w poszczególnych lokalizacjach?

7.  Czy klienci którzy wystawiają lepszą ocenę częściej kupują?

8.  Czy istnieją znaczące różnice w kategoriach i kwotach zakupów w zależności od lokalizacji i sezonu?

9.  Czy sezon wpływa na wartość zakupów w poszczególnych kategoriach?

10. Czy płeć wpływa na zakup produktów z poszczególnych kategorii?

11. Czy posiadanie subskrypcji wpływa na częstotliowść zakupów?

12. Jaka jest średnia ocen zakupów dla poszczególnych stanów?

13. Czy istnieją znaczące różnice w kategoriach produktów i kwotach zakupów w zależności od sezonu?

## Czy wiek konsumentów wpływa na wybór kategorii zakupionych przedmiotów?

::: {style="text-align: justify;"}
Aby odpowiedzieć na to pytanie badawcze przedstawiono skumulowany wykres słupkowy ilustrujący rozkład kategorii zakupionych produktów w podziale na grupy wiekowe. Grupy wiekowe zostały zdefiniowane jako: 18-29, 30-44, 45-59 oraz 60+. Wykres ten pozwala na wizualne porównanie udziału procentowego poszczególnych kategorii w różnych grupach wiekowych, co umożliwia ocenę wpływu wieku na wybór kategorii zakupionych przedmiotów.
:::

```{r}
dane_kompletne$age_group <- cut(dane_kompletne$Age, 
                           breaks = c(18, 30, 45, 60, Inf), 
                           labels = c("18-29", "30-44", "45-59", "60+"),
                           right = FALSE)
ggplot(data = dane_kompletne, aes(x=Category, fill = age_group)) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = scales::percent) +
  labs(
    title = "Wpływ wieku na kategorię zakupionych produktów",
    x = "Kategoria",
    y = "Udział procentowy",
    fill = "Grupa wiekowa"
  ) +
  theme_minimal() +
  scale_fill_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

::: {style="text-align: justify;"}
Powyższy wykres został uzupełniony o analizę statystyczną, która umożliwia ocenę istotności różnic w rozkładzie kategorii zakupionych produktów pomiędzy grupami wiekowymi. Wyniki analizy statystycznej wskazały na brak istotnych różnic pomiędzy kategorią a wiekiem. Wartość p dla testu chi-kwadrat, wyniosła 0.20, co jest znacznie powyżej przyjętego poziomu istotności, w związku z tym nie ma podstaw do odrzucenia hipotezy zerowej.
:::

```{r warning=FALSE}
ggbarstats(
  data = dane_kompletne,
  x = age_group,
  y = Category,
  bf.message = FALSE) +
  scale_fill_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

## Czy wiek konsumentów wpływa na wysokość kwoty zakupu?

::: {style="text-align: justify;"}
Do zbadani wpływu wieku konsumentów na wysokość kwoty zakupu wykorzystano wykres słupkowy przedstawiający wartość zakupu w poszczególnych grupach wiekowych. Wykres ten pozwala na wizualne porównanie średnich kwot zakupów pomiędzy różnymi grupami wiekowymi, co umożliwia ocenę wpływu wieku na wysokość wydatków konsumenckich. Grupy "30-44" i "45-59" przeciętnie wydawały najwięcej pieniędzy. Osoby powyżej 60 roku życia najmniej.
:::

```{r}
ggplot(data = dane_kompletne, aes(x=age_group, y=Amount)) +
  geom_col(fill = "#6094DB") +
  xlab("Wiek") +
  ylab("Kwota zakupu") +
  ggtitle("Kwota zakupu w zależności od wieku") +
  theme_light()
```

::: {style="text-align: justify;"}
Przeprowadzona analiza statystyczna wykazała, że wiek konsumentów nie ma istotnego wpływu na wysokość ich wydatków. Średnie wydatki w każdej z grup wiekowych są do siebie zbliżone, około 60 doloarów, a rozkłady wydatków charakteryzują się niemal identycznym rokzładem.
:::

```{r}
ggbetweenstats(
  data = dane_kompletne,
  x = age_group,
  y = Amount,
  title = "Rozkład wydatków według kategorii",
  xlab = "Grupa wiekowa",
  ylab = "Wydatki") +
  scale_colour_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

## Jakie są najpopularniejsze kombinacje rozmiarów i kolorów w poszczególnych kategoriach?

::: {style="text-align: justify;"}
Wykres przedstawia cztery kategorie: Accessories, Clothing, Footwear oraz Outerwear. Zastosowano wykres słupkowy wielokrotny, w którym na osi X znajdują się dostępne rozmiary (L, M, S, XL), natomiast kolory słupków odpowiadają rzeczywistej kolorystyce produktów. Najbardziej rozbudowaną kategorią są ubrania (Clothing), liczba obserwacji dla poszczególnych kombinacji jest najwyższa, sięgając ponad 40 sztuk dla wybranych kolorów w rozmiarze M. We wszystkich kategoriach, szczególnie w odzieży i akcesoriach, rozmiar M wyraźnie dominuje nad pozostałymi. Rozmiary S oraz L prezentują zbliżone do siebie udziały, podczas gdy rozmiar XL jest najrzadziej reprezentowany. Zbiór danych charakteryzuje się bardzo dużą paletą barw.W kategorii Clothing widać dużą popularność kolorów jaskrawych i nasyconych. Kategoria Footwear oraz Outerwear wykazuje znacznie mniejszą liczebność produktów, a rozkład kolorów jest bardziej wyrównany, a nieznacznie dominujace kolory należa do bardziej stonowanych.
:::

```{r}
ggplot(dane_kompletne, aes(x = Size, fill = Color)) +
  geom_bar(position = "dodge") +
  facet_wrap(~ Category) +
  scale_fill_manual(values = c(
    "Red"   = "red",
    "Blue"  = "blue",
    "Black" = "black",
    "White" = "white",
    "Green" = "green",
    "Beige" = "beige",
    "Brown" = "brown",
    "Charcoal" = "#36454F",
    "Cyan" = "#00FFFF",
    "Gold" = "#FFD700",
    "Gray" = "gray",
    "Indigo" = "#4B0082",
    "Lavender" = "#E6E6FA",
    "Magenta" = "#FF00FF",
    "Maroon" = "#800000",
    "Olive" = "#808000",
    "Orange" = "orange",
    "Peach" = "#FFE5B4",
    "Pink" = "pink",
    "Purple" = "purple",
    "Silver" = "#C0C0C0",
    "Teal" = "#008080",
    "Turquoise" = "#40E0D0",
    "Violet" = "#EE82EE",
    "Yellow" = "yellow"
  )) +
  labs(
    title = "Najpopularniejsze kombinacje rozmiarów i kolorów w kategoriach",
    x = "Rozmiar",
    y = "Ilość obserwacji",
    fill = "Kolor"
  ) +
  theme_minimal()
```

## Jakie są preferencje klientów dotyczące metod płatności w zależności od grupy wiekowej?

::: {style="text-align: justify;"}
Preferencje kielientów dotyczące metod płatności zostały przedstawione na wykresie słupkowym wielokrotnym, gdzie na osi X znajdują się różne metody płatności, a kolory słupków odpowiadają grupom wiekowym klientów. Klienci korzystają z sześciu różnych metod płatności: przelewu bankowego (Bank Transfer), gotówki (Cash), karty kredytowej (Credit Card), karty debetowej (Debit Card), systemu PayPal oraz Venmo.W niemal każdej kategorii płatności najliczniejszą grupę stanowią osoby w przedziałach wiekowych 30-44 oraz 45-59. Osoby powyżej 60. roku życia wykazują najniższą liczebność we wszystkich rodzajach płatności. Przy czym najczęściej wybierają kartę kredytową albo gotówkę.
:::

```{r}
ggplot(data = dane_kompletne, aes(x=Payment, fill = age_group)) +
  geom_bar(position = "dodge") +
  labs(
    x = "Metoda płatności",
    y = "Liczba klientów",
    fill = "Wiek") +
  ggtitle("Preferencje dotyczące metod płatności w zależności od grupy wiekowej") +
  theme_light() +
  scale_fill_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

::: {style="text-align: justify;"}
Wyniki testów przeprowadzonych dla całego zestawienia wskazują na brak istotnych różnic w preferencjach płatniczych pomiędzy badanymi grupami wiekowymi. Wartość p dla testu chi-kwadrat wyniosła 0.32, co jest powyżej przyjętego poziomu istotności, co sugeruje brak podstaw do odrzucenia hipotezy zerowej o braku związku między wiekiem a wyborem metody płatności. Jednak rozkład wewnątrz grup jest bardziej zróżnicowany. Niskie wartości $p$ wskazują, że wewnątrz konkretnych metod płatności udziały grup wiekowych istotnie różnią się od założonego rozkładu.
:::

```{r}
ggbarstats(
  data = dane_kompletne,
  x = age_group,
  y = Payment,
  bf.message = FALSE) +
  scale_fill_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

## Czy klienci wydający więcej wybierają szybsze opcje wysyłki?

```{r warning=FALSE}
ggplot(data = dane_kompletne, aes(x=Amount, y=Shipping)) +
  geom_col(fill = "#6094DB") +
  xlab("Wydatki") +
  ylab("Rodzaj wysyłki") +
  ggtitle("Rodzaj wysyłki w zależności od wydanej kwoty") +
  theme_light()

dane_kompletne$Shipping <- factor(dane_kompletne$Shipping)

ggplot(dane_kompletne, aes(x=Amount, y=Shipping)) +
  geom_boxplot(fill = "#DB6E60") +
  xlab("Wydatki") +
  ylab("Rodzaj wysyłki") 
```

## Czy większa liczba klientów wpływa na wyższą sprzedaż w poszczególnych lokalizacjach?

::: {style="text-align: justify;"}
Korelacja między całkowitą sprzedarząą a liczbą klientów w poszczególnych lokalizacjach została przedstawiona na wykresie punktowym. Na osi X znajduje się całkowita sprzedaż w danej lokalizacji, natomiast na osi Y liczba klientów, natomiast każdy punk odpowiada za jedną lokalizację Na wykresie widoczny jest trend wzrostowy. Wraz ze wzrostem liczby klientów w danej lokalizacji, proporcjonalnie rośnie całkowita sprzedaż.Punkty układają się wzdłuż linii diagonalnej, co sugeruje silną, liniową zależność między tymi zmiennymi. Potwierdza to wysoki wynik współczynnika korelacji Pearsona, który wynosi 0.88. Największe skupisko obserwacji znajduje się w środkowej części wykresu. Oznacza to, że większość lokalizacji osiąga zbliżone, "średnie" wyniki operacyjne. W prawym górnym rogu wykresu widoczne są punkty reprezentujące najbardziej dochodowe lokalizacje.
:::

```{r}
library(ggpubr)
dane_kompletne <- dane_kompletne %>%
  group_by(Location) %>%
  mutate(num_clients = n()) %>%
  ungroup()

dane_kompletne <- dane_kompletne %>%
  group_by(Location) %>%
  mutate(total_sale_per_location = sum(Amount)) %>%
  ungroup()


ggplot(data = dane_kompletne, aes(x=total_sale_per_location, y=num_clients)) +
  geom_point() +
  geom_smooth(method = "lm", color = "blue", se = TRUE) +
  stat_cor(method = "pearson", label.x = 4000, label.y = 95) +
  scale_x_continuous(name = "Całkowita sprzedaż w poszczególnych lokalizacjach") +
  scale_y_continuous(name = "Liczba klientów") +
  ggtitle("Całowita sprzedaż w relacji do liczby klientów") +
  theme_light()
```

## Czy klienci którzy wystawiają lepszą ocenę częściej kupują?

```{r}
dane_kompletne$rating_cat<-cut(dane_kompletne$Rating,
                breaks=c(-Inf, 1, 2, 3, 4, Inf),
                labels=c("Very Low", "Low", "Medium", "High", "Very High"))
ggplot(data = dane_kompletne, aes(y=Previous.Purchases)) +
geom_boxplot(aes(x=rating_cat, fill=rating_cat)) +
  labs(
  x = "Ocena",
  y = "Liczba poprzednich zakupów",
  fill = "Ocena") +
  ggtitle("Liczba poprzednich zakupów w zależności od oceny") +
  theme_light() +
  scale_fill_manual(values = c(
    "Very Low" = "#687586", 
    "Low" = "#5C504F", 
    "Medium" = "#6094DB", 
    "High" = "#DB6E60",   
    "Very High" = "#C1DB60" 
  ))
  
```

## Czy istnieją znaczące różnice w kategoriach i kwotach zakupów w zależności od lokalizacji i sezonu?

::: {style="text-align: justify;"}
Różnice w kategoriach i kwotach zakupów w zależności od lokalizacji zostały przedstawione na wykresie typu heatmap. Na osi X znajdują się cztery kategorie produktów, natomiast na osi Y lokalizacje. Kolory na wykresie reprezentują wartość zakupów, gdzie jaśniejsze odcienie wskazują niższe wartości, a ciemniejsze wyższe. Odzież wykazuje najwyższe zagęszczenie ciemnych pól w niemal wszystkich stanach, co sugeruje, że jest to najbardziej popularna kategioria. Obówie (Footwear) i odzież wierzchnia (Outwear) są kategoriamii na które przebadani konsumenci wydali mniejsze kwoty, wskazuje na to dominacja jaśniajszych kolorów.
:::

```{r, fig.height=12, fig.width=8}
ggplot(dane_kompletne, aes(x = Category, y = Location, fill = Amount)) +
  geom_tile(color = "white") +
  scale_fill_gradient(
    low = "#bce0ee",
    high = "#2E4565",
    name = "Wartość"
  ) +
  labs(
    title = "Wartość zakupów w poszczególnych kategoriach i lokalizacjach",
    x = "Kategoria",
    y = "Lokalizacja"
  ) +
  theme_minimal()

```

## Czy sezon wpływa na wartość zakupów w poszczególnych kategoriach?

::: {style="text-align: justify;"}
Wpływ pory roku na wartość zakupów w poszczególnych kategoriach został przedstawiony za pomocą wykresu interakcji. Według wykresu na obuwie badani konsumenci wydają najwięcej pieniędzy na jesień. Ta kategoria charakteryzuje się również dużym zróżnicowaniem pomiędzy porami roku. Na akcesorie największa suma pieniędzy zostaje przeznaczona na wiosnę. Natomiast ubrania charakteryzują się odrotnym trendem do akcesoriów. Odzież wierzchnia to kategoria o stosunkowo najniższych wahaniach. Choć jej wartość rośnie od wiosny do lata, przez resztę roku utrzymuje trend lekko spadkowy, osiągając najniższy poziom zimą. Wykres wyraźnie wskazuje na wpływ sezonowości na wartość zakupów w poszczególnych kategoriach. W celu sprawdzenia czy zaobserwowane różnice są istotne statystycznie została przeprowadzona test ANOVA.
:::

```{r}
dane_interakcja <- dane_kompletne %>%
  group_by(Season, Category) %>%
  summarise(mean_amount = mean(Amount), .groups = 'drop')


ggplot(dane_interakcja, aes(x = Season, y = mean_amount, color = Category, group = Category)) +
  geom_point(size = 3) +
  geom_line(size = 1.2) +
  labs(title = "Czy sezon wpływa na wartość zakupów w kategoriach?",
       x = "Pora roku",
       y = "Średnia wartość zakupu",
       color = "Kategoria produktu") +
  theme_minimal() +
  theme(legend.position = "bottom") +
  scale_x_discrete(limits = c("Spring", "Summer", "Fall", "Winter"))+
  scale_color_manual(values = c("Accessories" = "#6094DB", 
                                "Clothing" = "#DB6E60", 
                                "Footwear" = "#C1DB60", 
                                "Outerwear" = "#5C504F"))
```

::: {style="text-align: justify;"}
Otrzymane wyniki analizy wariancji (ANOVA) wskazują na iistotną statystycznie interakcję między porą roku a kategorią produktu. Oznacza to, że wpływ sezonu na wartość zakupów nie jest stały — to, czy rednio od tego, jaką grupę produktów analizujemy. Potwierdza to wizualne obserwacje z wykresu interakcji, gdzie linie trendów przecinały się, zamiast biec równolegle. Brak statystycznie istotnego wpływu zmiennej Season na zmienną Amount, oznacza, że jeśli uśrednimy wydatki ze wszystkich kategorii, pory roku same generują znacząco różnych poziomów sprzedaży. Tak samo brak wpływu zmiennej Category na Amount, wskazuje, że w skali całego roku, żadna kategoria nie jest droższa lub tańsza od pozostałych.
:::

```{r}
model_pory_roku <- aov(Amount ~ Season * Category, data = dane_kompletne)

wyniki_tidy_pory_roku <- tidy(model_pory_roku)

wyniki_tidy_pory_roku %>%
  kable(
    caption = "Tabela 1: Wyniki analizy wariancji dla zmiennej Amount",
    col.names = c("Efekt", "Df", "Suma kwadratów", "Średni kwadrat", "Statystyka F", "p-value"),
    digits = 3 # Zaokrąglenie do 3 miejsc po przecinku
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "responsive"),
    full_width = F,
  ) %>%
  row_spec(0, bold = TRUE, color = "white", background = "#6094DB") 

```

## Czy płeć wpływa na zakup produktów z poszczególnych kategorii?

```{r}
dane_kompletne %>%
  count(Gender, Category) %>%
  ggplot(aes(x = Gender, 
             y = Category,
             fill = n)) +
  labs(title = "Zakupy w poszczególnych kategoriach z podziałem na płeć",
       x = "Płeć",
       y = "Kategoria",
       fill = "Liczba zakupów") +
  geom_tile() +
  geom_label(aes(label = n), color = "white") +
  scale_fill_gradient(low = "#bce0ee", 
                      high = "#2E4565")


```

## Czy posiadanie subskrypcji wpływa na częstotliowść zakupów?

```{r}
dane_kompletne %>%
  count(Subscription, Frequency) %>%
  ggplot(aes(x = Subscription, 
             y = Frequency,
             fill = n)) +
  labs(title = "Częstotliwość zakupów w zależności od posiadania subskrypcji",
       x = "Posiadanie subskrypcji",
       y = "Częstotliwość zakupów",
       fill = "Liczba zakupów") +
  geom_tile() +
  geom_label(aes(label = n), color = "white") +
  scale_fill_gradient(low = "#ffd4cc", 
                      high = "#DB6E60")


```

## Średnia ocen zakupów w poszczególnych stanach

::: {style="text-align: justify;"}
Średnia ocen zakupów w poszczególnych stanach została przedstawiona na mapie Stanów Zjednoczonych. Kolory na mapie reprezentują średnią ocenę zakupów w danym stanie, gdzie odcienie czerwieni wskazują niższe średnie oceny, a odcienie zieleni wyższe. Regionami o najwyższej satysfakcji z zakupów to takie stany jak Teksas (TX), Wisconsin (WI) oraz Waszyngton (WA). Wyraźnie niższe oceny widoczne są w stanach Oklahoma (OK), Georgia (GA) oraz Wirginia Zachodnia (WV). Średnia w tych lokalizacjach zbliża się do dolnej granicy skali (3.6). Stany w centralnej części USA (np. Nebraska, Kansas) oraz na północnym wschodzie (np. Maine, Vermont) prezentują umiarkowany, stabilny poziom satysfakcji (kolory żółte i jasnozielone).
:::

```{r warning=FALSE}
avg_rating_per_location <- dane_kompletne %>%
  group_by(Location) %>%
  summarize(Srednia = mean(Rating))

dane_mapa <- avg_rating_per_location %>% 
  rename(state = Location)


ggplotly(
plot_usmap(data = dane_mapa, values = "Srednia", color = "white", labels = TRUE) + 
  scale_fill_continuous(low = "#DC143C", high = "#adfc03", name = "Średnia ocena", label = scales::comma) + 
  theme(legend.position = "right") +
  labs(title = "Średnia ocena zakupów w poszczególnych stanach"))

```

## Korelacje zmiennych jakościowych

::: {style="text-align: justify;"}
Dla badanego zbioru danych przeprowadzono analizę korelacji pomiędzy zmiennymi jakościowymi za pomocą współczynnika Craméra V. Współczynnik ten mierzy siłę związku pomiędzy dwiema zmiennymi kategorycznymi, przyjmując wartości od 0 (brak związku) do 1 (pełna zależność).
:::

```{r, include=FALSE, warning=FALSE}
# Cramer's V calculation --------------------------------------------------

nowe_dane <- dane_kompletne[,c(3,4,5,7,8,9,10,12,13,14,15,17,18)]
cat_var <- colnames(nowe_dane)
# Function to compute Cramer's V
cv.test = function(x,y) {
  CV = sqrt(chisq.test(x, y, correct=FALSE)$statistic /
              (length(x)[1] * (min(length(unique(x))[1],length(unique(y))[1]) - 1)))
  return(as.numeric(CV))
}

# Apply the function to the combination of categorical variable
v_cramer_all <- function(cat_var, df){
  cat_var_grid <- data.table(combinations(n = length(cat_var), r = 2, v = cat_var, repeats.allowed = FALSE))
  
  do.call(rbind,
          apply(cat_var_grid, 1, function(x){
            tmp <- as.character(x)
            vec1 <- unlist(df[,tmp[1],])
            vec2 <- unlist(df[,tmp[2],])
            
            data.table(
              variable_x = tmp[1],
              variable_y = tmp[2],
              chi2 = chisq.test(x = vec1, vec2, correct=FALSE)$p.value,
              v_cramer = cv.test(x = vec1, y = vec2)
            )
          }))
  
}

results <- v_cramer_all(cat_var = cat_var, df = nowe_dane)

```

```{r}
# Heatmap vizualisation with ggplot2  -------------------------------------

g <- ggplot(results, aes(variable_x, variable_y)) +
  geom_tile(aes(fill = v_cramer), colour = "black") +
  theme(axis.text.x=element_text(angle=45, hjust=1)) +
  scale_fill_gradient(low = "white", high = "#6094DB") +
  theme_bw() + xlab(NULL) + ylab(NULL) +
  theme(axis.text.x=element_text(angle = -90, hjust = 0)) +
  ggtitle("Cramer's V heatmap")

ggplotly(g)
```

## Korelacja zmiennych ilościowych

```{r warning=FALSE}
korelacja_macierz <- cor(dane_kompletne[,c(2,6,11,16)])
corrplot(korelacja_macierz, method="color")
```

# Wnioskowanie statystyczne

## Analiza porównawcza kwot wydatków w podziale na kategorie

```{r warning=FALSE}
ggbetweenstats(
  data = dane_kompletne,
  x = Category,
  y = Amount,
  title = "Rozkład wydatków według kategorii",
  xlab = "Kategoria produktu",
  ylab = "Wydatki") +
  scale_colour_manual(values = c("#6094DB", "#DB6E60","#C1DB60", "#5C504F"))
```

## Dwuskładnikowa analiza ANOVA

```{r}
wyniki <- aov(Amount ~ Rating*Gender, data = dane_kompletne)
wyniki_tidy <- tidy(wyniki)

wyniki_tidy %>%
  kable(
    caption = "Tabela 2:Wyniki analizy wariancji dla zmiennej Amount",
    col.names = c("Efekt", "Df", "Suma kwadratów", "Średni kwadrat", "Statystyka F", "p-value"),
    digits = 3 # Zaokrąglenie do 3 miejsc po przecinku
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "responsive"),
    full_width = F,
  ) %>%
  row_spec(0, bold = TRUE, color = "white", background = "#6094DB") 

```

## Analiza różnic w wydatkach między kobietami a mężczyznami względem ocen

```{r}
interaction.plot(dane_kompletne$Rating, dane_kompletne$Gender, dane_kompletne$Amount,
                 xlab = "Ocena zakupu",
                 ylab = "Kwota zakupu",
                 trace.label = "Płeć",
                 col = c("#DB6E60", "#6094DB"),
                 lty = 1,
                 lwd = 2,
) 
```

## Analiza wariancji wpływu oceny i płci na wysokość wydatków

```{r}
dane_kompletne %>%
  anova_test(Amount ~ Rating*Gender) %>%
  kable(
    caption = "Tabela 3: Wpływ oceny i płci na wysokość wydatków",
    col.names = c("Efekt", "Dfn", "DFd", "statystyka F", "p-value", "p<0,05", "ges"),
    digits = 3 # Zaokrąglenie do 3 miejsc po przecinku
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "responsive"),
    full_width = F,
  ) %>%
  row_spec(0, bold = TRUE, color = "white", background = "#6094DB") 
                
```

## Czy istnieje zależność między płcią a kategorią zakupionych przedmiotów?

```{r warning=FALSE}
ggbarstats(
  data = dane_kompletne,
  x = Gender,
  y = Category,
  bf.message = FALSE) +
  scale_fill_manual(values = c("#6094DB", "#DB6E60"))
```

## Analiza ANCOVA

```{r}
dane_kompletne |>
  aov(Amount ~ Rating*Gender+Age, data = _) |>
  tidy() |>
kable(
    caption = "Tabela 4: Wyniki analizy ANCOVA",
    col.names = c("Efekt", "Df", "Suma kwadratów", "Średni kwadrat", "Statystyka F", "p-value"),
    digits = 3 # Zaokrąglenie do 3 miejsc po przecinku
  ) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "responsive"),
    full_width = F,
  ) %>%
  row_spec(0, bold = TRUE, color = "white", background = "#6094DB") 
```
